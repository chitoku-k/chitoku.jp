language: node_js
node_js:
  - '11'

env:
  global:
    - TZ=Asia/Tokyo

cache:
  directories:
    - node_modules
    - plugins/gatsby
    - plugins/gatsby-remark-component

before_install:
  - .travis/before_install

before_script:
  - .travis/before_script

script:
  - yarn build

after_success:
  - .travis/after_success
